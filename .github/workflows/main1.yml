name: second-workflow
on: 
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: greeting
        run: echo "hello welcome" 
      - name: Knowledge transfer
        run: |
          echo "hello KT-1"
          echo "hellp KT-2"
